<%= form_for [@consumer, @meter] do |f| %>
	<%= f.label :name, "Name" %>
	<%= f.text_field :name %>
	<%= f.label :k_trans, "Coef. of transform." %>
	<%= f.number_field :k_trans %>
	<%= f.label :alarm_value, "Alarm value" %>
	<%= f.number_field :alarm_value %>
	<%= f.label :alarm_on, "Alarm on" %>
	<%= f.check_box :alarm_on %>

	<%= f.submit %>
<% end %>